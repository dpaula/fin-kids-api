spring:
  datasource:
    url: "jdbc:mysql://${DB_HOST:panel.autevia.com.br}:${DB_PORT:3306}/${DB_NAME:fin-kids-db}?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: ${DB_USERNAME:finkids}
    password: '${DB_PASSWORD:FinKids@2026#Secure}'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true

app:
  security:
    automation:
      token: ${N8N_API_TOKEN:dev-automation-token}
